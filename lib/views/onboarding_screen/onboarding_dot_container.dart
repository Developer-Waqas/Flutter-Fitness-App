import '../../constants/app_linker/app_linker.dart';

class OnboardingDotContainer extends StatelessWidget {
  const OnboardingDotContainer({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}