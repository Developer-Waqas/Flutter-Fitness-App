import '../../constants/app_linker/app_linker.dart';

class ForotPasswordScreen extends StatelessWidget {
  const ForotPasswordScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}